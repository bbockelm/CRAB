#
# CRAB-Proxy makefile
# $Id: Makefile_2.6,v 1.2 2010/01/15 14:58:00 spiga Exp $
# Fabio Farina 2007/12/10 

.PHONY:all install

# Things for python 2.6 

boost_cflags=-g -pthread -fPIC -I/usr/include/libxml2 \
	       -DBOOST_PYTHON_DYNAMIC_LIB -I$(BOOST_ROOT)/include \
               -I$(PYTHON_ROOT)/include/python2.6 -I. 

boost_libs=-lxml2 -lpthread -lpython2.6 \
             -L/opt/globus/lib -L$(BOOST_ROOT)/lib -L$(PYTHON_ROOT)/lib \
	     -Wl,-rpath-link,. -L$(PYTHON_ROOT)/lib/python2.6/config/ -lboost_python

cli_objs = server2.o soapC.o soapServer.o stdsoap2.o

# building rules
.c.o: 
	gcc -c $(boost_cflags) $< -o $@

.cpp.o:
	g++ -c $(boost_cflags) $< -o $@

all: FrontendLoader_1_1.o $(cli_objs)
	g++ -o FrontendLoader_1_1.so -module -shared $(boost_libs) -L. -Wl,-R. FrontendLoader_1_1.o $(cli_objs)

install:
	echo "Installing server-side libs"
	mkdir -p ../bin/server
	cp FrontendLoader_1_1.so ../bin/server

clean:
	rm -f *.o  $(O_NAME) *.so

rebuild:
	clean all

